# Ropacal Backend - Environment Variables Template
# Copy this file to .env and fill in your actual values

# Application Settings
APP_SHARED_PASSWORD=ropacal123
PORT=8080

# JWT Authentication
# Generate a strong secret: openssl rand -base64 32
APP_JWT_SECRET=your_secret_key_here_change_in_production

# PostgreSQL Database Connection
# Format: postgres://username:password@hostname:port/database_name?sslmode=disable
# Local development example:
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ropacal?sslmode=disable

# Production example (use sslmode=require):
# DATABASE_URL=postgres://user:pass@db.example.com:5432/ropacal?sslmode=require

# Firebase Cloud Messaging (Optional - for push notifications)
# Get credentials from: https://console.firebase.google.com/ → Project Settings → Service Accounts

# Option 1: File path (best for local development)
FIREBASE_CREDENTIALS_FILE=./firebase-service-account.json

# Option 2: Base64-encoded credentials (best for Railway, Fly.io, Render)
# Encode your JSON: cat firebase-service-account.json | base64
# Then paste the output here:
# FIREBASE_CREDENTIALS_BASE64=ewogICJ0eXBlIjogInNlcnZpY2VfYWNjb3VudCIsC...
